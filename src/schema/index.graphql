#import User from './User.graphql";
#import Postagem from './Postagem.graphql';

type Query {
  getUsers: [User]!
  getUser(id: ID): User
  geradorNumerosMega: [Int!]!
  getPostagens: [Postagem]
  getPostagem(postId: Int): [Postagem]
}
